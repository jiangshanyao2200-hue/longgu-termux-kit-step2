# startboot

`startboot/` 里的所有可执行 `*.sh` 文件都会被 `motd` 随机抽取播放。

约定：

- 只要是 `*.sh` 且带执行权限，就会进入随机池，文件名不限。
- 脚本出错、超时或异常退出时，启动链会直接跳过，不会阻塞进入主页。
- 如果你需要放说明文件、素材或草稿，不要命名成可执行 `*.sh`。
- 动画脚本要尽量自己适配终端尺寸变化；当前模板脚本已经支持在键盘弹出、窗口高度变化时动态裁切。
