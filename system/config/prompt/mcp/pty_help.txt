Terminal（PTY）已启动：
- Home：打开/隐藏 Terminal 视图（后台继续运行）
- Esc：发送给终端内程序（vim/less/cancel 等）
- Esc×2（{DOUBLE_ESC_MS}ms 内）：结束当前终端任务（终止 PTY）
- PgUp/PgDn：切换不同终端任务（多 PTY tab）
- Alt+↑：复制当前终端快照到聊天输入框（由用户决定是否发送给 AI）
