测试中发现 

给dog发消息会显示到main的聊天区域。
将他们做完全隔离

他们目前已经 上下文隔离 窗口隔离
但三者共享一份fastmemory在上下文工程

需要确定 他们的上下文皆为prompt fastmemory 和外置context
其中只有fastmemory是共享 另外两个为独立

然后不同标签页 显示 UI 样式保持统一
但要确保逻辑正确
在哪个标签页对话 就显示在哪个标签页
杜绝这种在dog发消息用户消息被显示到main哪里
确保他们的窗口都是独立的
只是共享一个输入框

在输入框输入/的bug又出现了 输入完后不会自动弹出 需要用户点击一下屏幕才会出现，但这是一开始才会，后续就不会这样了。是个极小的bug

关于pty 用户自己打开pty 输入完后 按esc结束pty 也会将结果汇报给AI 但明确标注这是用户自己执行的，在pty汇总汇报时引入确保能清晰提示 这个pid是用户自己运行的

settings界面的提示词管理，可以管理PROMPT文件夹下所有提示词。然后 编辑界面也要支持触控移动光标。进入编辑界面后 上下左右的方向键还有delete，要作用于编辑界面。 ctrl+s保存 esc退出不保存。如果用户修改了提示词 未保存 就要退出，弹出确认界面，当前改动未保存，是否退出。设计好这个确认界面，未来我们会用到这个确认界面。很多地方。 弹出界面做个简单的动画，增加流畅感。 用户可以点击选择yes or no 也可以方向键左右选择回车确认。