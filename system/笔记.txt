[自我感知]1/10 Date 3% ↑0.00M ↓0.00M T↑6.54M T↓2.72M ♡33 ♥15 这里date尾部和↑应该有空格间隔开
·Powered by AItermux
这里下方新增一行顶栏，我们做标签页 做什么标签页呢，做窗口切换。分别为 Matrix/WatchDog/Memory 分别作为三个窗口，分别对应main dog memory api 他们现在各自独占一个标签页 这三个是固定标签页 支持触控 也支持按shift+← →切换。然后输入框shift enter 应该为换行 alt enter应该为强制发送到队列 ctrl+enter 也应该是换行
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
♡ · 系统信息 · 欢迎使用AItermux。
  build: release

  Project Ying（萤）

  欢迎。你在一个终端聊天里使用 AI 助手。 这里有个多余的build release 。 把欢迎语设为所有快捷键介绍。 
  然后移除/ switch to 这个功能 和相关代码，主聊天区域main不再支持切换dog ，顺便移除切换dog等系统提示 而是每个因此独立。顺便移除testmemory和相关test逻辑。 当处于不同标签页时 消息发送给不同的api。一并移除上方顶栏展开memory的所有展开和显示逻辑 因为不再需要 ，而是分标签页取代。标签页需要是动态可变的 如果WatchDog活跃 就显示标签动画，动画的设计要赛博朋克。如果memory在活跃 ，就显示memory动画，如果matrix活跃标签页也动画。 
  然后顶栏的·Powered by AItermux 改为 状态栏 用于显示 标签页动态 如 
  ●NOW Working · Matrix · WatchDog · Memory 
  只有正在活跃的显示 ，同时活跃就用· 间隔 如果同时活跃的多了从●NOW Working · 开始左右移动显示 以最右侧的时间为间隔 显示不要超过时间将时间挤压或者覆盖
  
  这些操作我们移除了 顶栏memory的展开逻辑 移除了test memory逻辑  移除了Switch to逻辑 确保这些被完全清理 我们的新方案稳定落地
  